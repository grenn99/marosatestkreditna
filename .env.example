# Client-safe environment variables (can be committed to git)
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# IMPORTANT: The following variables should ONLY be in .env.local (not committed to git)
# and should NEVER be deployed to production or included in the frontend code
# NEVER prefix sensitive variables with VITE_ as that would expose them in the frontend
# SUPABASE_SERVICE_KEY=your-supabase-service-key
# STRIPE_SECRET_KEY=your-stripe-secret-key
# DATABASE_URL=your-database-url
# JWT_SECRET=your-jwt-secret
